# AgentCrew plugin examples

This directory contains example plugins for AgentCrew:

- [`local-example.py`](local-example.py) — a single-file plugin demonstrating
  activation, EventBus subscription, and `tool.execute` hook registration.

See [`PLUGIN_DEVELOPMENT.md`](../../PLUGIN_DEVELOPMENT.md) for the plugin authoring contract.
